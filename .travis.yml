# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r
language: r
r:
  - oldrel
  - release
  - devel
jobs:
  include:
    - os: linux
      dist: trusty
    - os: osx
      osx_image: xcode7.2
sudo: false
cache: packages
warnings_are_errors: true
os:
  - linux
  - osx